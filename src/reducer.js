const initialState = {
  currenQuestion: `question`
}

const ActionCreator = {

}

const reducer = (state = initialState , action) => {
  switch (action.type) {
    case 
  }

  return state;
};

export {reducer, ActionCreator};